PORT=5000
MONGODB_URI=
JWT_SECRET=Bsa@123456
JWT_EXPIRES_IN=1d

# Frontend URL (for password reset links, "View Product" links in emails)
FRONTEND_URL=http://localhost:3000

# Backend URL (for product image links in welcome email; optional locally, set in production e.g. https://your-api.com)
# BACKEND_URL=http://localhost:5000

# SMTP Configuration for Email
# For Gmail: You MUST use App Password (not regular password)
# 1. Enable 2-Factor Authentication on Google Account
# 2. Generate App Password: https://myaccount.google.com/apppasswords
# 3. Use the 16-character app password (remove spaces)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-character-app-password-no-spaces
SMTP_FROM_NAME=Soft Chilli